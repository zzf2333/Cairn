## USER
How should we handle caching for our most frequently accessed API endpoints?

## USER
No, we tried Redis before and it was a disaster — constant connection drops, config drift across environments, and our 2-person ops team spent half their time babysitting it. Use in-memory caching instead.
