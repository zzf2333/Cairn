description: |
  Cold-start: project has no Cairn cognition yet. AI must call cairn_init_status, detect
  the empty state, and follow the AI-native initialization path (analyze project, then call
  cairn_init_commit with dry_run before any final write).

required_tool_calls:
  # Accept either init_status (explicit init probe) or context (which also surfaces uninit state).
  - tool: cairn_context
    must_be_called: true
    description: must consult Cairn before answering

required_text_patterns:
  - pattern: "(?i)not.*initial|empty|scaffold|init|initial.*cognition|首次|未.*初始化|没有发现|没有.*代码|没有.*源码|no.*source|no.*code|没.*应用|没有.*实际|nothing.*here"
    description: must communicate the empty / uninitialized state to the user

forbidden_text_patterns:
  - pattern: "(?i)the team strongly prefers|the codebase is known to|historically the team has"
    description: must not fabricate project history when none exists yet
