config:
  project_name: scenario-c1
  domains: [auth]
  cognitive_mode: standard

state:
  stage: { phase: growth, confidence: 0.7, status: advisory }

skeleton:
  - domain: auth
    role: authentication
    owns: [login]
    causal_keywords: [auth, login]
